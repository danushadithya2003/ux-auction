services:
  - type: web
    name: ux-auction
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --workers 1 --threads 8 --worker-class gthread --timeout 120 --bind 0.0.0.0:$PORT app:app
    autoDeploy: true
